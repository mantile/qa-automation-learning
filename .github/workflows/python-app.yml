name: Python application

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  setup:
    runs-on: ubuntu-22.04
    outputs:
      setup-complete: ${{ steps.setup.outputs.complete }}
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Install Playwright browsers
        run: |
          playwright install --with-deps chromium

      - name: Verify setup
        id: setup
        run: |
          python -c "import playwright; print('Playwright installed')"
          python -c "import requests; print('Requests installed')"
          echo "complete=true" >> $GITHUB_OUTPUT

  banking-smoke-tests:
    needs: setup
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: 'pip'
      - run: pip install -r requirements.txt
      - run: playwright install chromium
      
      - name: Check and run BankingProject Smoke tests
        id: banking_smoke
        run: |
          MARKER="banking and smoke"
          TEST_COUNT=$(pytest --collect-only -m "$MARKER" -q 2>/dev/null | grep -c "test_" || true)
          echo "Found $TEST_COUNT tests for marker: $MARKER"
          echo "test_count=$TEST_COUNT" >> $GITHUB_OUTPUT
          
          if [ "$TEST_COUNT" -gt 0 ]; then
            pytest -m "$MARKER" -v --tb=short
          else
            echo "No tests found, skipping..."
          fi

  banking-ui-tests:
    needs: setup
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: 'pip'
      - run: pip install -r requirements.txt
      - run: playwright install chromium
      
      - name: Check and run BankingProject UI tests
        id: banking_ui
        run: |
          MARKER="banking and ui"
          TEST_COUNT=$(pytest --collect-only -m "$MARKER" -q 2>/dev/null | grep -c "test_" || true)
          echo "Found $TEST_COUNT tests for marker: $MARKER"
          echo "test_count=$TEST_COUNT" >> $GITHUB_OUTPUT
          
          if [ "$TEST_COUNT" -gt 0 ]; then
            pytest -m "$MARKER" -v --tb=short
          else
            echo "No tests found, skipping..."
          fi

  banking-api-tests:
    needs: setup
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: 'pip'
      - run: pip install -r requirements.txt
      
      - name: Check and run BankingProject API tests
        id: banking_api
        run: |
          MARKER="banking and api"
          TEST_COUNT=$(pytest --collect-only -m "$MARKER" -q 2>/dev/null | grep -c "test_" || true)
          echo "Found $TEST_COUNT tests for marker: $MARKER"
          echo "test_count=$TEST_COUNT" >> $GITHUB_OUTPUT
          
          if [ "$TEST_COUNT" -gt 0 ]; then
            pytest -m "$MARKER" -v --tb=short
          else
            echo "No tests found, skipping..."
          fi

  saucedemo-smoke-tests:
    needs: setup
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: 'pip'
      - run: pip install -r requirements.txt
      - run: playwright install chromium
      
      - name: Check and run Saucedemo Smoke tests
        id: saucedemo_smoke
        run: |
          MARKER="saucedemo and smoke"
          TEST_COUNT=$(pytest --collect-only -m "$MARKER" -q 2>/dev/null | grep -c "test_" || true)
          echo "Found $TEST_COUNT tests for marker: $MARKER"
          echo "test_count=$TEST_COUNT" >> $GITHUB_OUTPUT
          
          if [ "$TEST_COUNT" -gt 0 ]; then
            pytest -m "$MARKER" -v --tb=short
          else
            echo "No tests found, skipping..."
          fi

  saucedemo-ui-tests:
    needs: setup
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: 'pip'
      - run: pip install -r requirements.txt
      - run: playwright install chromium
      
      - name: Check and run Saucedemo UI tests
        id: saucedemo_ui
        run: |
          MARKER="saucedemo and ui"
          TEST_COUNT=$(pytest --collect-only -m "$MARKER" -q 2>/dev/null | grep -c "test_" || true)
          echo "Found $TEST_COUNT tests for marker: $MARKER"
          echo "test_count=$TEST_COUNT" >> $GITHUB_OUTPUT
          
          if [ "$TEST_COUNT" -gt 0 ]; then
            pytest -m "$MARKER" -v --tb=short
          else
            echo "No tests found, skipping..."
          fi

  saucedemo-api-tests:
    needs: setup
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: 'pip'
      - run: pip install -r requirements.txt
      
      - name: Check and run Saucedemo API tests
        id: saucedemo_api
        run: |
          MARKER="saucedemo and api"
          TEST_COUNT=$(pytest --collect-only -m "$MARKER" -q 2>/dev/null | grep -c "test_" || true)
          echo "Found $TEST_COUNT tests for marker: $MARKER"
          echo "test_count=$TEST_COUNT" >> $GITHUB_OUTPUT
          
          if [ "$TEST_COUNT" -gt 0 ]; then
            pytest -m "$MARKER" -v --tb=short
          else
            echo "No tests found, skipping..."
          fi

  restfulbooker-smoke-tests:
    needs: setup
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: 'pip'
      - run: pip install -r requirements.txt
      - run: playwright install chromium
      
      - name: Check and run RestfulBooker Smoke tests
        id: restfulbooker_smoke
        run: |
          MARKER="restfulbooker and smoke"
          TEST_COUNT=$(pytest --collect-only -m "$MARKER" -q 2>/dev/null | grep -c "test_" || true)
          echo "Found $TEST_COUNT tests for marker: $MARKER"
          echo "test_count=$TEST_COUNT" >> $GITHUB_OUTPUT
          
          if [ "$TEST_COUNT" -gt 0 ]; then
            pytest -m "$MARKER" -v --tb=short
          else
            echo "No tests found, skipping..."
          fi

  restfulbooker-ui-tests:
    needs: setup
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: 'pip'
      - run: pip install -r requirements.txt
      - run: playwright install chromium
      
      - name: Check and run RestfulBooker UI tests
        id: restfulbooker_ui
        run: |
          MARKER="restfulbooker and ui"
          TEST_COUNT=$(pytest --collect-only -m "$MARKER" -q 2>/dev/null | grep -c "test_" || true)
          echo "Found $TEST_COUNT tests for marker: $MARKER"
          echo "test_count=$TEST_COUNT" >> $GITHUB_OUTPUT
          
          if [ "$TEST_COUNT" -gt 0 ]; then
            pytest -m "$MARKER" -v --tb=short
          else
            echo "No tests found, skipping..."
          fi

  restfulbooker-api-tests:
    needs: setup
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: 'pip'
      - run: pip install -r requirements.txt
      
      - name: Check and run RestfulBooker API tests
        id: restfulbooker_api
        run: |
          MARKER="restfulbooker and api"
          TEST_COUNT=$(pytest --collect-only -m "$MARKER" -q 2>/dev/null | grep -c "test_" || true)
          echo "Found $TEST_COUNT tests for marker: $MARKER"
          echo "test_count=$TEST_COUNT" >> $GITHUB_OUTPUT
          
          if [ "$TEST_COUNT" -gt 0 ]; then
            pytest -m "$MARKER" -v --tb=short
          else
            echo "No tests found, skipping..."
          fi

  report:
    needs:
      - banking-smoke-tests
      - banking-ui-tests
      - banking-api-tests
      - saucedemo-smoke-tests
      - saucedemo-ui-tests
      - saucedemo-api-tests
      - restfulbooker-smoke-tests
      - restfulbooker-ui-tests
      - restfulbooker-api-tests
    runs-on: ubuntu-22.04
    if: always()
    
    steps:
      - name: Check all tests passed
        run: |
          echo "All test groups completed!"
          echo "Check the Actions tab for detailed results"